{
  "name": "modelswap-extension",
  "version": "2.0.0",
  "description": "MODELSWAP — Chrome extension that bridges MODELSWAP to the user's logged-in browser sessions",
  "private": true,
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.260",
    "typescript": "^5.4.0"
  }
}
